include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: false

analyzer:
  errors:
    # Generated/test ergonomics — keep the build green without nit-picking.
    avoid_print: ignore
